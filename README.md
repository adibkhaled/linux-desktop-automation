# V-Center testing

Test cases are invoked as part of the integration test, during a nightly build.
In the Linux repository (linux-desktop), the CI job template is called
`.integration-test`. Job `integration-test-nightly` is a scheduled job,
and `integration-test` can be run manually.



